<script>
   //unobstrusive: attach method call here
   //call when typing or enter or focus leaving
   $( document ).ready(function() {

    function doSearch() {
      $.ajax({
        url: "<%= landlords_filter_path %>",
        data: {
          filter : $('#code_name_filter').val()
        },
        dataType: "script"
      }).fail(function() {
        //console.log("[FAILED] search update failed");
      });
    };

    var timer;
    $('#code_name_filter').keyup(function() {
      clearInterval(timer);  //clear any interval on key up
      timer = setTimeout(doSearch, 500);
    });
  });
</script>

<% provide(:title, 'Landlords') %>
<h1 class="page-header">Landlords</h1>

<div class="row">
  <div class="col-md-6">
    <%= form_tag landlords_path, :method => 'get' do %>
      <%= label_tag(:code_name_filter, "Filter:") %>
      <%= text_field_tag :code_name_filter, params[:code_name], 
        placeholder: "code/name", size: 20 %>
      &nbsp;&nbsp;
      <%= label_tag(:agent, "Primary Agent:") %>
      <%= text_field_tag :agent, params[:agent], placeholder: "Agent Name", size: 20 %>
      &nbsp;&nbsp;
      <%= check_box_tag(:checkbox_active, class: "checkbox inline") %>
      <%= label_tag :checkbox_active, "Has active units" %>
    <% end %>

  </div>
  <div class="col-md-3 pull-right">
    <%= link_to 'Add a Landlord', new_landlord_path %>
    <%= will_paginate @landlords %>
  </div>
</div>

<div class="row">
  <div class="col-md-12">

    <table class="table table-striped table-hover table-condensed table-responsive">
      <thead>
        <tr>
          <th>Code</th>
          <th>Name</th>
          <th>Mobile</th>
          <th>Active Units</th>
          <th>Total Units</th>
          <th>Last Unit Updated</th>
          <th colspan="1"></th>
        </tr>
      </thead>

      <tbody class="landlords_table_body">
        <%= render @landlords %>
      </tbody>
    </table>

  </div>
</div>