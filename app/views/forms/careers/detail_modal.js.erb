$('#entryDetailModal .modal-title').html(
	"<%= j(@entry.name) %> - Additional Details");

$('#entryDetailModal .modal-body').html(
	"<p><b>Where do you live?</b></p><%= j(@entry.what_neighborhood_do_you_live_in) %><br /><br /><%= j(link_to 'Resume', @entry.resume_upload) %>");


$('#entryDetailModal').modal('show');

// update 'read' counts
$('.careers_table_body').html("<%= escape_javascript (render partial: 'forms/careers/entry', :collection => @entries, as: :entry) %>");
$('.totals').html("<%= @entries.count %> Entries");
$('.careers_badge').html("<%= unread_careers_count %>");
$('.total_forms_badge').html("<%= total_unread_forms_count %>");