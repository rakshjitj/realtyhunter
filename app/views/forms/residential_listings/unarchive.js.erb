$('.form_listing_table_body').html("<%= escape_javascript (render partial: 'forms/residential_listings/entry', :collection => @entries, as: :entry) %>");
$('.totals').html("<%= @entries.total_count %> Entries");
