$('.contact_us_table_body').html("<%= escape_javascript (render partial: 'forms/contact_us/entry', :collection => @entries, as: :entry) %>");
$('.totals').html("<%= @entries.total_count %> Entries");
