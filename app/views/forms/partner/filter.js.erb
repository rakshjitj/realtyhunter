$('.contact_us_table_body').html("<%= escape_javascript (render partial: 'forms/contact_us/wufoo_contact_us_form', :collection => @entries, as: :entry) %>");

$('.pagination').html("<%= escape_javascript (render partial: 'shared/pagination', locals: {:models => @entries}) %>");

$('.totals').html("<%= @entries.count %> Entries");
