$('#entryUnarchiveModal .delete-btn').replaceWith("<%= j(link_to 'Mark Active', unarchive_modal_forms_partner_path(@entry), remote: true, method: :post, class: 'btn btn-danger link-as-button', id: 'delete-link') %>");
$('#entryUnarchiveModal #delete-link').click( function(event) {
	$('#entryUnarchiveModal').modal('hide');
	event.preventDefault();
});

$('#entryUnarchiveModal').modal('show');
