$('.form_listing_table_body').html("<%= escape_javascript (render partial: 'forms/commercial_listings/entry', :collection => @entries, as: :entry) %>");
$('.totals').html("<%= @entries.count %> Entries");
