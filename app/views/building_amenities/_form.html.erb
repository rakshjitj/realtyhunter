<%= form_for(@building_amenity) do |f| %>
  <% if @building_amenity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@building_amenity.errors.count, "error") %> prohibited this building_amenity from being saved:</h2>

      <ul>
      <% @building_amenity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
