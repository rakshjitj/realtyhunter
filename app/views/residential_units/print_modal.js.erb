$('.print-private-btn').click( function(event) {
	// show spinner
   $(this).toggleClass('active');
   //$('#printModal btn').disabled = true;
 	// no need to hide modal because the page redirects
});

$('.print-public-btn').click( function(event) {
	// show spinner
	$(this).toggleClass('active');
	// no need to hide modal because the page redirects
});
