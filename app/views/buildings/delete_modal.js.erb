$('#bldgDeleteModal .modal-body').html("<%= j(render :partial => "delete_modal_helper", locals: { building: @building }) %>");

// would use button_to but can't seem to style the form...
//$('#bldgDeleteModal .delete-btn').replaceWith("<%= j(button_to 'Delete', @building, remote: true, method: :delete, class: 'btn btn-danger') %>");
$('#bldgDeleteModal .delete-btn').replaceWith("<%= j(link_to 'Delete', @building, remote: true, method: :delete, class: 'btn btn-danger link-as-button', id: 'delete-link') %>");
$('#bldgDeleteModal #delete-link').click( function(event) {
	console.log('sdffdsfds');
	$('#bldgDeleteModal').modal('hide');
	event.preventDefault();
});

$('#bldgDeleteModal').modal('show');
