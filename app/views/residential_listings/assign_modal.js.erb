$('#unitAssignModal .modal-body').html("<%= j(render :partial => "assign_modal_helper", locals: { listings: @listings }) %>");

$('#unitAssignModal .btn-submit').click( function(event) {
	$('#unitAssignModal').modal('hide');
	Listings.hideSpinner();
});

$('#unitAssignModal').modal('show');
