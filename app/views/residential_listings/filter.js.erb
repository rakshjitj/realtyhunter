$('.totals').html("<%= @residential_units.total_count %> Units found");

$('.residential_units_table_body').html("<%= escape_javascript (render :partial => @residential_units) %>");

$('.pagination').html("<%= escape_javascript (render :partial => 'shared/pagination', locals: {:models => @residential_units}) %>");

ResidentialListings.updateOverviewMap("<%= escape_javascript(@map_infos).to_s.html_safe %>");

$('#residential .announcements').html("<%= j(render partial: "residential_listings/announcements", locals: { announcements: @announcement_items }) %>");